# Test text

```
The quick brown fox jumps over the lazy dog
John took the VA Tank forever.
snake_case and camelCase
Test and moreTest
function(){}
if (a == b && b === c && c != d && d !== e) {}
if (e >= 2 || f <= 3.99) { __private(); }
(...args) => args
\a \n \T \' \\
foo_Ta
x -> x-x
a += 3 -= 2 *= 9 /= 11
x--
run -- --option
{ foo: 'bar' }
999 >> 1, 1 << 999 <<< inf
5 - 5, 3 + 3, 6 * 6, 99 / 99
/var/opt/../usr/path.file
/pattern/**/*.*
// Comments
/**
 * Block comments
 */
Foo\\Bar
Foo::bar
$quux_baz
$foo ?: $bar
```
